export default state => next => action => {
	console.log('---, logger action',action)
	console.log('---, logger state prev', state.getState())
	next(action)
	console.log('---, logger state next', state.getState())
}