import {LOAD_PRODUCTS} from 'constans'

function loadProducts(state = [], action){
	const {type} = action;
	switch(type){
		case LOAD_PRODUCTS:
			console.log('---, reducer load')
			return state
		default:
			return state;
	}
}

export default loadProducts;